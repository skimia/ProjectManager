{{module_var}}.controller('{{base_name}}.IndexController',['$scope','Announcements','Projects', 'Bundles',function($scope, Announcements, Projects, Bundles){
	$scope.announcements = Announcements.query();
    $scope.projects = Projects.query();
    $scope.bundles = Bundles.query();
}]);

{{module_var}}.controller('{{base_name}}.AboutController',['$scope','$rootScope',function($scope, $rootScope){
    $scope.getRevision = function(link){
        return link.split("/")[6];
    };
}]);